apiVersion: v1
kind: Service
metadata:
  name: ai-browser-backend
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: ai-browser-backend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.backend.port }}
      nodePort: {{ .Values.service.nodePort }}
